FROM node:18-alpine

RUN apk add --no-cache \
    python3 \
    make \
    g++ \
    cairo-dev \
    jpeg-dev \
    pango-dev \
    giflib-dev \
    curl

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

RUN mkdir -p auth_info data logs

EXPOSE 3000

CMD ["npm", "run", "dev"]
